language: r
sudo: false
cache: packages

r:
  - oldrel
  - release
  - devel

r_github_packages:
    - mllg/checkmate
    - tudo-r/BatchJobs
    - jimhester/covr
    - rstats-db/RSQLite

after_success:
    - Rscript -e 'library(covr); coveralls()'

notifications:
  email:
    on_success: change
    on_failure: always
